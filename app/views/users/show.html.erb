
<div class="card">
<div class="card-header"> Auteur </div>
<div class="card-body">
  <h5 class=""><%= @user.first_name%> <%= @user.last_name%></h5>
  <h5 class="card-subtitle text-muted">Age: <%= @user.age%></h5>
  <h5 class="card-subtitle text-muted">Qui nous vient de: <%= link_to "#{@user.city.name}", city_path(@user.city.id)%></a></h5>
  <blockquote class="blockquote mb-0">
    <p>Sa description: <%= @user.description%></p>
    <footer class="blockquote-footer">Contact: <%= @user.email%></footer>
  </blockquote>
</div>
</div>


